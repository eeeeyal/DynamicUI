# 📦 מדריך מבנה קובץ ZIP - Dynamic UI Runtime Engine

## 📋 תוכן עניינים
1. [מבנה כללי](#מבנה-כללי)
2. [קבצי הגדרה](#קבצי-הגדרה)
3. [קבצי מסכים](#קבצי-מסכים)
4. [תיקיית Assets](#תיקיית-assets)
5. [דוגמאות](#דוגמאות)
6. [כללים והגבלות](#כללים-והגבלות)

---

## 📁 מבנה כללי

קובץ ה-ZIP צריך להכיל את המבנה הבא:

```
your_app.zip
├── app.json              (חובה) - הגדרות כלליות של האפליקציה
├── routes.json           (חובה) - מיפוי של כל המסכים
├── styles.json           (אופציונלי) - הגדרות עיצוב
├── actions.json          (אופציונלי) - הגדרות פעולות
├── screens/              (חובה) - תיקיית מסכים
│   ├── home.json
│   ├── screen1.json
│   ├── screen2.json
│   └── ...
└── assets/               (אופציונלי) - קבצי מדיה
    ├── logo.png
    ├── images/
    └── ...
```

---

## 📄 קבצי הגדרה

### 1. `app.json` (חובה)

קובץ ההגדרות הראשי של האפליקציה.

**פורמט:**
```json
{
  "appId": "my-app",
  "version": "1.0.0",
  "initialRoute": "home",
  "rtl": false
}
```

**שדות:**
- `appId` (string, חובה) - מזהה ייחודי של האפליקציה
- `version` (string, חובה) - גרסת האפליקציה (למשל: "1.0.0")
- `initialRoute` (string, חובה) - המסך הראשי שמוצג בהפעלה (חייב להתאים למפתח ב-`routes.json`)
- `rtl` (boolean, אופציונלי) - האם האפליקציה תומכת בכתיבה מימין לשמאל (ברירת מחדל: `false`)

**דוגמה:**
```json
{
  "appId": "demo-app",
  "version": "1.0.0",
  "initialRoute": "home",
  "rtl": true
}
```

---

### 2. `routes.json` (חובה)

מיפוי של כל המסכים באפליקציה. כל מפתח הוא מזהה המסך, והערך הוא הנתיב לקובץ ה-JSON של המסך.

**פורמט:**
```json
{
  "routeId1": "screens/screen1.json",
  "routeId2": "screens/screen2.json",
  "routeId3": "screens/screen3.json"
}
```

**כללים:**
- כל מפתח חייב להיות ייחודי
- הנתיבים יחסיים לשורש ה-ZIP
- הנתיבים חייבים להתחיל ב-`screens/`
- אין הגבלה על מספר המסכים (תומך ב-100+ מסכים)

**דוגמה:**
```json
{
  "home": "screens/home.json",
  "settings": "screens/settings.json",
  "transactions": "screens/transactions.json",
  "profile": "screens/profile.json"
}
```

---

### 3. `styles.json` (אופציונלי)

הגדרות עיצוב כלליות של האפליקציה.

**פורמט:**
```json
{
  "primaryColor": "#4f46e5",
  "secondaryColor": "#7c3aed",
  "backgroundColor": "#f3f4f6",
  "buttonRadius": 12
}
```

**שדות:**
- `primaryColor` (string, אופציונלי) - צבע ראשי (פורמט hex: `#RRGGBB`, ברירת מחדל: `#4f46e5`)
- `secondaryColor` (string, אופציונלי) - צבע משני (ברירת מחדל: `#7c3aed`)
- `backgroundColor` (string, אופציונלי) - צבע רקע (ברירת מחדל: `#f3f4f6`)
- `buttonRadius` (number, אופציונלי) - רדיוס פינות כפתורים בפיקסלים (ברירת מחדל: `12`)

**דוגמה:**
```json
{
  "primaryColor": "#6366f1",
  "secondaryColor": "#8b5cf6",
  "backgroundColor": "#ffffff",
  "buttonRadius": 8
}
```

---

### 4. `actions.json` (אופציונלי)

הגדרות פעולות שניתן לבצע באפליקציה (ניווט, קריאות API, וכו').

**פורמט:**
```json
{
  "actionId1": {
    "type": "navigation",
    "route": "targetRouteId"
  },
  "actionId2": {
    "type": "api",
    "method": "POST",
    "endpoint": "/api/endpoint"
  }
}
```

**סוגי פעולות:**

#### פעולת ניווט (`navigation`)
```json
{
  "goToSettings": {
    "type": "navigation",
    "route": "settings"
  }
}
```

#### פעולת API (`api`)
```json
{
  "submitForm": {
    "type": "api",
    "method": "POST",
    "endpoint": "/api/submit"
  }
}
```

**דוגמה מלאה:**
```json
{
  "goToHome": {
    "type": "navigation",
    "route": "home"
  },
  "goToSettings": {
    "type": "navigation",
    "route": "settings"
  },
  "goToTransactions": {
    "type": "navigation",
    "route": "transactions"
  },
  "submitTransaction": {
    "type": "api",
    "method": "POST",
    "endpoint": "/api/transaction"
  }
}
```

---

## 🖼️ קבצי מסכים

כל מסך מוגדר בקובץ JSON נפרד בתיקיית `screens/`.

### מבנה קובץ מסך

**פורמט בסיסי:**
```json
{
  "type": "screen",
  "id": "screen-id",
  "appBar": {
    "title": "כותרת המסך"
  },
  "body": {
    "type": "column",
    "children": [
      // רכיבי המסך כאן
    ]
  }
}
```

**שדות:**
- `type` (string, חובה) - חייב להיות `"screen"`
- `id` (string, חובה) - מזהה ייחודי של המסך (חייב להתאים למפתח ב-`routes.json`)
- `appBar` (object, אופציונלי) - הגדרות AppBar
  - `title` (string) - כותרת המסך
- `body` (object, חובה) - תוכן המסך (רכיב שורש)

---

### סוגי רכיבים

#### 1. `column` - עמודה אנכית
```json
{
  "type": "column",
  "children": [
    // רכיבים כאן
  ]
}
```

#### 2. `row` - שורה אופקית
```json
{
  "type": "row",
  "children": [
    // רכיבים כאן
  ]
}
```

#### 3. `text` - טקסט
```json
{
  "type": "text",
  "value": "טקסט להצגה",
  "fontSize": 16,
  "fontWeight": "bold",
  "color": "#000000"
}
```

**שדות:**
- `value` (string, חובה) - הטקסט להצגה
- `fontSize` (number, אופציונלי) - גודל גופן (ברירת מחדל: `16`)
- `fontWeight` (string, אופציונלי) - משקל גופן (`"normal"`, `"bold"`, `"light"`)
- `color` (string, אופציונלי) - צבע טקסט (פורמט hex)

#### 4. `button` - כפתור
```json
{
  "type": "button",
  "text": "לחץ כאן",
  "action": "goToSettings",
  "backgroundColor": "#6366f1",
  "textColor": "#ffffff"
}
```

**שדות:**
- `text` (string, חובה) - טקסט הכפתור
- `action` (string, אופציונלי) - מזהה פעולה מ-`actions.json`
- `backgroundColor` (string, אופציונלי) - צבע רקע (ברירת מחדל: צבע ראשי)
- `textColor` (string, אופציונלי) - צבע טקסט (ברירת מחדל: `#ffffff`)

#### 5. `image` - תמונה
```json
{
  "type": "image",
  "asset": "assets/logo.png",
  "height": 120,
  "width": 200
}
```

**שדות:**
- `asset` (string, חובה) - נתיב לתמונה (יחסי לשורש ה-ZIP)
- `height` (number, אופציונלי) - גובה בפיקסלים
- `width` (number, אופציונלי) - רוחב בפיקסלים

#### 6. `form` - טופס
```json
{
  "type": "form",
  "fields": [
    {
      "type": "text",
      "label": "שם",
      "name": "name",
      "required": true
    }
  ],
  "actions": [
    {
      "type": "button",
      "text": "שלח",
      "action": "submitForm"
    }
  ]
}
```

---

### דוגמה למסך מלא

**`screens/home.json`:**
```json
{
  "type": "screen",
  "id": "home",
  "appBar": {
    "title": "מסך ראשי"
  },
  "body": {
    "type": "column",
    "children": [
      {
        "type": "image",
        "asset": "assets/logo.png",
        "height": 120
      },
      {
        "type": "text",
        "value": "ברוך הבא למערכת",
        "fontSize": 24,
        "fontWeight": "bold"
      },
      {
        "type": "button",
        "text": "הגדרות",
        "action": "goToSettings"
      },
      {
        "type": "button",
        "text": "עסקאות",
        "action": "goToTransactions"
      }
    ]
  }
}
```

---

## 🎨 תיקיית Assets

תיקיית `assets/` מכילה קבצי מדיה (תמונות, אייקונים, וכו').

**מבנה מומלץ:**
```
assets/
├── logo.png
├── images/
│   ├── banner.jpg
│   └── icon.png
└── icons/
    └── app-icon.png
```

**שימוש בתמונות:**
בקבצי המסכים, השתמש בנתיב יחסי:
```json
{
  "type": "image",
  "asset": "assets/logo.png"
}
```

או:
```json
{
  "type": "image",
  "asset": "assets/images/banner.jpg"
}
```

---

## 📝 דוגמאות

### דוגמה 1: אפליקציה פשוטה

**מבנה ZIP:**
```
simple_app.zip
├── app.json
├── routes.json
├── styles.json
├── actions.json
└── screens/
    ├── home.json
    └── about.json
```

**`app.json`:**
```json
{
  "appId": "simple-app",
  "version": "1.0.0",
  "initialRoute": "home",
  "rtl": false
}
```

**`routes.json`:**
```json
{
  "home": "screens/home.json",
  "about": "screens/about.json"
}
```

**`styles.json`:**
```json
{
  "primaryColor": "#1976D2",
  "secondaryColor": "#424242",
  "backgroundColor": "#FFFFFF",
  "buttonRadius": 8
}
```

**`actions.json`:**
```json
{
  "goToAbout": {
    "type": "navigation",
    "route": "about"
  }
}
```

**`screens/home.json`:**
```json
{
  "type": "screen",
  "id": "home",
  "appBar": {
    "title": "בית"
  },
  "body": {
    "type": "column",
    "children": [
      {
        "type": "text",
        "value": "ברוך הבא!",
        "fontSize": 24,
        "fontWeight": "bold"
      },
      {
        "type": "button",
        "text": "אודות",
        "action": "goToAbout"
      }
    ]
  }
}
```

---

### דוגמה 2: אפליקציה עם 100 מסכים

**מבנה ZIP:**
```
large_app.zip
├── app.json
├── routes.json
├── styles.json
├── actions.json
└── screens/
    ├── home.json
    ├── screen1.json
    ├── screen2.json
    ├── ...
    └── screen99.json
```

**`routes.json` (דוגמה חלקית):**
```json
{
  "home": "screens/home.json",
  "screen1": "screens/screen1.json",
  "screen2": "screens/screen2.json",
  "screen3": "screens/screen3.json",
  ...
  "screen99": "screens/screen99.json"
}
```

**הערה:** האפליקציה תומכת במספר בלתי מוגבל של מסכים!

---

## ✅ כללים והגבלות

### כללים כלליים

1. **שמות קבצים:**
   - קבצי JSON חייבים להיות בפורמט UTF-8
   - שמות קבצים יכולים להיות באנגלית, עברית, או כל תו תומך
   - מומלץ להשתמש בשמות קבצים קטנים וללא רווחים

2. **מבנה JSON:**
   - כל קבצי ה-JSON חייבים להיות תקינים (valid JSON)
   - אין תמיכה בהערות (`//` או `/* */`) ב-JSON
   - שדות אופציונליים יכולים להיעדר, אבל שדות חובה חייבים להיות קיימים

3. **מזהים:**
   - כל `id` במסכים חייב להיות ייחודי
   - כל `route` ב-`routes.json` חייב להיות ייחודי
   - כל `action` ב-`actions.json` חייב להיות ייחודי

4. **נתיבים:**
   - כל הנתיבים יחסיים לשורש ה-ZIP
   - נתיבים לתמונות חייבים להתחיל ב-`assets/`
   - נתיבים למסכים חייבים להתחיל ב-`screens/`

### הגבלות

1. **גודל קבצים:**
   - אין הגבלה רשמית על גודל ה-ZIP
   - מומלץ לשמור על קבצי JSON קטנים (עד כמה KB כל אחד)
   - תמונות גדולות עלולות להאט את הטעינה

2. **מספר מסכים:**
   - אין הגבלה על מספר המסכים
   - האפליקציה תומכת ב-100+ מסכים ללא בעיות ביצועים

3. **עומק קינון:**
   - אין הגבלה על עומק הקינון של רכיבים
   - מומלץ לשמור על מבנה ברור וקריא

---

## 🔍 בדיקות לפני יצירת ZIP

לפני יצירת ה-ZIP, ודא:

- [ ] `app.json` קיים ותקין
- [ ] `routes.json` קיים ותקין
- [ ] כל המסכים ב-`routes.json` קיימים בתיקיית `screens/`
- [ ] כל ה-`id` במסכים תואמים למפתחות ב-`routes.json`
- [ ] כל ה-`action` בכפתורים קיימים ב-`actions.json` (אם מוגדר)
- [ ] כל הנתיבים לתמונות תקינים וקיימים
- [ ] כל קבצי ה-JSON תקינים (ניתן לבדוק ב-[JSONLint](https://jsonlint.com/))

---

## 📚 משאבים נוספים

- [JSON Specification](https://www.json.org/)
- [Flutter Documentation](https://flutter.dev/docs)

---

**גרסה:** 1.0.0  
**תאריך עדכון:** 2024

